# Dependências do Atlas
#
# MÓDULO 01: nenhuma. Usamos só a biblioteca padrão do Python.
#
# Isso é proposital. Antes de usar pandas para agrupar um DataFrame, você
# precisa entender o que ele faz por baixo — dicionário, laço e agregação.
#
# Instale mesmo assim, para pegar o hábito:
#     pip install -r requirements.txt
#
# Este arquivo cresce a cada módulo:
#
#   M04  ->  pydantic
#   M05  ->  psycopg[binary], SQLAlchemy, alembic, pymongo
#   M06  ->  fastapi, uvicorn[standard], python-jose, passlib[bcrypt]
#   M07  ->  httpx, redis, tenacity
#   M10  ->  pandas, polars, pyarrow, beautifulsoup4, selenium
#
# ---------------------------------------------------------------------------
# MÓDULO 10 — o pipeline de dados
#
# Descomente conforme for chegando em cada etapa do ROTEIRO_M10.md.
#
# pandas==2.2.3          # etapa 2 em diante
# pyarrow==17.0.0        # 🔴 obrigatório para Parquet — pandas não lê
#                        #    parquet sozinho, e o erro que ele dá quando
#                        #    falta ("Unable to find a usable engine") não
#                        #    é nada óbvio
# polars==1.9.0          # etapa 5, se você optar por lazy
# beautifulsoup4==4.12.3 # só se a origem for HTML
# lxml==5.3.0            # parser rápido para o bs4
#
# ⚠️ `selenium` NÃO está aqui de propósito. Ele baixa um navegador
#    inteiro e só se justifica quando a página monta o conteúdo por
#    JavaScript. Antes de instalá-lo, confira se a página não tem uma
#    API JSON por trás — quase sempre tem, e aí o httpx do M07 resolve.
#
# ---------------------------------------------------------------------------
# Ferramentas de desenvolvimento (opcionais, mas recomendadas já no M01).
# Descomente se quiser usar:
#
# ruff==0.6.9          # linter + formatador, rápido
# mypy==1.11.2         # verificação estática de type hints
